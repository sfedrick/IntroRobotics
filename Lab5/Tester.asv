clc;
clear;
params=[1,3,3,1];
goal=[50,100,20];
pos=[ 263     1   104];
stringmap="map1.txt";

plotvectors3d(stringmap,params, goal)
scale=1;
window=10;
plotVectorAtPoint(pos,goal,stringmap,params, scale,window)
M=loadmap(stringmap);
[origin,pr]=circleObstacle(M.obstacles)
Repulsive(pos,obstacle,RepStrength,poScale)